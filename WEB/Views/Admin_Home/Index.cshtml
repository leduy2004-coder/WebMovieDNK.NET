@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";  // Sử dụng layout admin của trang
}

<!-- Thêm thư viện Chart.js từ CDN -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<!-- Thêm thư viện FontAwesome từ CDN -->
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">

<div class="container">
    <div class="my-4">
        <label for="yearSelect" class="form-label">Chọn năm</label>
        <select id="yearSelect" class="form-select" onchange="updateData()">
            <option value="2024">2024</option>
            <option value="2023">2023</option>
            <!-- Thêm các năm khác -->
        </select>
    </div>

    <div class="row">
        <div class="col-md-8">
            <h5 class="mb-3 title">Tổng số vé được bán trong từng tháng</h5>
            <canvas id="salesChart"></canvas>
        </div>
        <div class="col-md-4">
            <h5 class="mb-3">Tổng quát</h5>
            <ul class="list-group">
                <li class="list-group-item">
                    <i class="fa-solid fa-ticket"></i> Tổng số vé bán được: <span id="totalTickets">112</span>
                </li>
                <li class="list-group-item">
                    <i class="fa-solid fa-money-bill"></i> Tổng doanh thu: <span id="totalRevenue">17,900,000</span>
                </li>
                <li class="list-group-item">
                    <i class="fa-solid fa-film"></i> Tổng phim: <span id="totalMovies">5</span>
                </li>
            </ul>
        </div>
    </div>

    <h5 class="mt-4 title">Tài khoản đặt vé nhiều nhất</h5>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Tên người dùng</th>
                <th>Số điện thoại</th>
                <th>Số vé đã đặt</th>
                <th>Tổng tiền</th>
            </tr>
        </thead>
        <tbody id="topUsers">
            <tr>
                <td>Lê D (D090000004@gmail.com)</td>
                <td>0900000004</td>
                <td>42</td>
                <td>2,240,000</td>
            </tr>
            <tr>
                <td>Lê C (C090000003@gmail.com)</td>
                <td>0900000003</td>
                <td>28</td>
                <td>1,260,000</td>
            </tr>
            <tr>
                <td>Lê E (E090000005@gmail.com)</td>
                <td>0900000005</td>
                <td>21</td>
                <td>1,400,000</td>
            </tr>
        </tbody>
    </table>

</div>
<style>
    .modal {
        display: flex;
        flex-direction: column;
        background: white;
        border-radius: 8px;
        padding: 20px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .list-group-item {
        display: flex;
        align-items: center;
        gap: 10px;
    }

    .container {
        background: aliceblue;
    }
    .title {
        margin: 27px 10px;
        font-size: 25px;
        color: #1e2651;
    }
</style>


<script>
    const ctx = document.getElementById('salesChart').getContext('2d');
    const salesChart = new Chart(ctx, {
        type: 'bar',
        data: {
            labels: ['Tháng 1', 'Tháng 2', 'Tháng 3', 'Tháng 4', 'Tháng 5', 'Tháng 6', 'Tháng 7', 'Tháng 8', 'Tháng 9', 'Tháng 10', 'Tháng 11', 'Tháng 12'],
            datasets: [{
                label: 'Số vé bán được',
                data: [10, 20, 0, 30, 40, 0, 0, 0, 50, 0, 0, 20],
                backgroundColor: 'rgba(54, 162, 235, 0.6)',
                borderColor: 'rgba(54, 162, 235, 1)',
                borderWidth: 1
            }]
        },
        options: {
            scales: {
                y: {
                    beginAtZero: true
                }
            }
        }
    });

    function updateData() {
    
        const year = document.getElementById('yearSelect').value;
        console.log(`Dữ liệu cho năm ${year}`);
       
    }
</script>

